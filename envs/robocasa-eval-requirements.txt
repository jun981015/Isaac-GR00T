# RoboCasa-only eval runtime dependencies.
#
# This env is for the RoboCasa simulator / ZMQ client process only. GR00T model
# inference, torch CUDA, and flash-attn should live in a separate policy-server
# env. Local source checkouts are installed separately with `pip install -e
# <path> --no-deps` so their setup.py dependency constraints do not fight each
# other.

numpy==1.23.5
mujoco==3.2.6
numba==0.57.1
llvmlite==0.40.1

pyzmq==27.1.0
msgpack==1.1.2
pandas==2.0.3
imageio==2.34.2
imageio-ffmpeg==0.6.0
termcolor==3.2.0
tqdm==4.67.1

opencv-python-headless==4.11.0.86
scipy==1.11.4
h5py==3.12.1
lxml==5.3.0
Pillow==10.4.0
PyYAML==6.0.2

pygame==2.6.1
pynput==1.8.1
hidapi==0.14.0.post4

qpsolvers[quadprog]==4.8.1
quadprog==0.1.13
mink==0.0.5
